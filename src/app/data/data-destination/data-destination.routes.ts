import { DataDestinationComponent } from './data-destination.component';
import { Routes } from '@angular/router';

export const DataDestinationRoutes: Routes = [
  { path: 'data-destination', component: DataDestinationComponent },
];
